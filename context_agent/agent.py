import pandas as pd\n\nclass ContextAgent:\n    def __init__(self, csv_path):\n        self.csv_path = csv_path\n        self.data = None\n\n    def read_csv(self):\n        """ Reads a CSV file and stores the data in a DataFrame. """\n        self.data = pd.read_csv(self.csv_path)\n        return self.data\n\n    def analyze_what_and_why(self):\n        """ Analyzes the data to extract insights (WHAT) and reasons (WHY). """\n        if self.data is None:\n            raise ValueError('Data not loaded. Please run read_csv() first.')\n\n        # Example analysis - you can customize this\n        what_analysis = self.data.describe()\n        why_analysis = {}  # Implement your own logic for WHY\n        return what_analysis, why_analysis\n\n    def enrich_from_jira_confluence(self):\n        """ Enriches the data with information from Jira and Confluence. """\n        # Placeholder for logic to fetch data from Jira and Confluence\n        # This could involve API calls to Jira/Confluence to get relevant data\n        enriched_data = {}  # Populate with enriched data\n        return enriched_data\n